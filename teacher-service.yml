server:
  port: 8200
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
zuul-proxy-server:
  ribbon:
    eureka:
      enabled: false
    listOfServers: localhost:8080 #,localhost:8092,localhost:8093
    ServerListRefreshInterval: 3000

